#include <iostream>
#nclud <stack>

using namespace std;

template < T ,T>
class mp{
	T maxp;
	T ss ; //stack
}

class maxProd{
	int max;
	stack ss;

};

class MatrixMove{

void moveMartix(matrix, stack ss)
{
	//check if reached the end
	if(mat[i][j] == ){
		printStack();

	}
	//move down
	maveMatrix(i+1,j,ss);
    //moveright
	moveMatix(i,j+1,ss);
}
void greatestProduct()
{

}
std::shared_ptr<char**> sh_ptr;
std::unique<char[][]> mat;
std::stack<pair(int,int)> ss;
mp<int, stack> mmax;

};
 //all usecases of shared pointer

main()
{
	//create matrix

	//search the end of it;
}